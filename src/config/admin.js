// Admin always lives on the same site as the deploy.
// No cross-domain redirect — works on any Netlify subdomain or custom domain.
export const ADMIN_PATH = '/admin'
export const ADMIN_URL = ADMIN_PATH
