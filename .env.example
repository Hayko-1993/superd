# Copy to .env for local `netlify dev` only — never commit real secrets.
# On Netlify: Site configuration → Environment variables (or CLI: netlify env:import .env)

# Required for login / API
SUPABASE_URL=https://jhvpbcakzcukrsjecvvn.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_secret_here

# Optional overrides (defaults exist in code if unset)
# ADMIN_EMAIL=Remy
# ADMIN_PASSWORD=465385AUA
# TELEGRAM_TOKEN=
# Comma-separated chat IDs for everyone who should get alerts (not phone numbers)
# TELEGRAM_CHAT_ID=603922771,8583389596

# Do not set VITE_API_URL on Netlify — leave blank so the site calls its own domain.
